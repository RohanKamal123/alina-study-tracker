# Optional. The app works fully offline (localStorage) with these unset.
# Fill them in only when you want cloud sync between phone and laptop.
# Get both values from Supabase -> Project Settings -> Data API / API Keys.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
